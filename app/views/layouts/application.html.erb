<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><a href="#">We're here!</a></h1>
        </li>
         <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <li class="has-dropdown">
            <% if user_signed_in? %>
              <a href="#">Logged in as <%= current_user.email %></a>
              <ul class="dropdown">
                <li><a href="#"><%= link_to "Log out", destroy_user_session_path, method: :delete %></a></li>
              </ul>
            <% else %>
              <li class="<%= controller.controller_name == 'sessions' ? 'active' : '' %>"><%= link_to "Log in", new_user_session_path %></li>
              <li class="<%= controller.controller_name == 'registrations' ? 'active' : '' %>"><%= link_to "Create an account", new_user_registration_path %></li>
            <% end %>
          </li>
        </ul>

      </section>
    </nav>

    <%= yield %>

  </body>
</html>
